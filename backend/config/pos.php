<?php

return [
    /*
    |--------------------------------------------------------------------------
    | スタッフ共通パスワード
    |--------------------------------------------------------------------------
    |
    | /order（客用）以外の全画面で使う共通ログインパスワード。
    | ログイン成功時はこのパスワードから導出したトークンを返し、
    | 以降は Authorization: Bearer <token> で認証する（有効期限なし）。
    |
    */
    'staff_password' => env('STAFF_PASSWORD', 'gakusai2026'),
];
